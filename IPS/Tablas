CREATE TABLE "EVENTOS"(
	EV_ID int NOT NULL,
	EV_NOMBRE varchar(255),
	EV_TIPO varchar(255),
	EV_DISTANCIA decimal,
	EV_PLAZAS_TOTALES int,
	EV_FINALIZADO int,
	EV_CATEGORIAS longvarchar,
	EV_PLAZOS_INS longvarchar,
	EV_FECHA_COMIENZO date,
	EV_ID_ORGANIZADOR int NOT NULL,
	EV_ETAPAS int,
	PRIMARY KEY (EV_ID)
	);
CREATE TABLE "ATLETA"
(
	AT_DNI varchar(255) PRIMARy KEY NOT NULL,
	AT_NOMBRE varchar(255),
	AT_SEXO int,
	AT_FECHA_NACIMIENTO varchar(255)
);
CREATE TABLE "INSCRIPCION"(
	AT_DNI  varchar(255) NOT NULL,
	EV_ID int NOT NULL,
	INS_ESTADO int,
	INS_FECHA_INS date,
	INS_RESULTADOS_SEG  longvarchar,
	INS_DORSAL int,
	INS_CATEGORIA varchar(255),
	PRIMARY KEY(AT_DNI,EV_ID),
	CONSTRAINT FK_EV_ID FOREIGN KEY(EV_ID) REFERENCES PUBLIC."EVENTOS" (EV_ID),
	CONSTRAINT FK_AT_DNI FOREIGN KEY(AT_DNI) REFERENCES PUBLIC."ATLETA" (AT_DNI),
);

CREATE TABLE "ORGANIZADOR"(
	ORG_ID int NOT NULL,
	ORG_NOMBRE vachar(255) NOT NULL,
	);

